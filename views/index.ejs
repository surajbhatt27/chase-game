<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Chess game</title>

    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

    <style>
        body { 
            margin: 0; 
            font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial; 
        }

        .chessboard {
            display: grid;
            grid-template-columns: repeat(8, 1fr);
            grid-template-rows: repeat(8, 1fr);
            width: 400px;
            height: 400px;
            background: transparent;
            border-radius: 8px;
            overflow: hidden;
        }

        .square { display: flex; align-items: center; justify-content: center; user-select: none; }

        .square.light { background-color: #f0d9b5; }

        .square.dark { background-color: #b58863; }

        .piece { font-size: 36px; cursor: grab; }

        .piece.draggable { cursor: grab; }

        .flipped { transform: rotate(180deg); }

        .flipped .piece { transform: rotate(180deg); }

        .highlight { 
            outline: 3px solid rgba(0,0,0,0.18); 
            box-shadow: inset 0 0 0 3px rgba(255,255,0,0.08); 
        }

        .move-list { 
            max-height: 400px; 
            overflow-y: auto; 
            padding: 8px; font-size: 14px; 
            background: rgba(255,255,255,0.02); 
            border-radius: 6px; 
        }

        .status { 
            font-weight: 600; 
            margin-bottom: 6px; 
        }
        .turn-dot { 
            width:10px; 
            height:10px; 
            border-radius:999px; 
            display:inline-block; 
            margin-right:6px; 
            vertical-align:middle; 
        }

        .piece.white {
            color: white;
        }

        .piece.black {
            color: black;
        }

    </style>
</head>
<body class="bg-zinc-900 text-slate-100 min-h-screen flex items-center justify-center p-6">
    <div class="w-full max-w-4xl mx-auto grid grid-cols-1 md:grid-cols-3 gap-6">
        <div class="md:col-span-2 flex items-center justify-center">
            <div class="chessboard-container">
                <div class="chessboard"></div>
            </div>
        </div>

        <div class="md:col-span-1 space-y-4">
            <div class="p-4 bg-zinc-800 rounded-lg shadow-inner">
                <div class="status" id="status">Loading...</div>
                <div class="text-sm mb-2">You are: <span id="myRole">Spectator</span></div>
                <div class="text-sm">Turn: <span id="turnIndicator"><span class="turn-dot" id="turnDot"></span><span id="turnText">-</span></span></div>
            </div>

            <div class="p-4 bg-zinc-800 rounded-lg">
                <div class="text-sm font-semibold mb-2">Move History</div>
                <div class="move-list" id="moveList"></div>
            </div>

            <div class="p-4 bg-zinc-800 rounded-lg flex gap-2">
                <button id="resetBtn" class="px-3 py-2 bg-emerald-600 rounded hover:opacity-90">Reset Board</button>
                <button id="flipBtn" class="px-3 py-2 bg-sky-600 rounded hover:opacity-90">Flip View</button>
            </div>
        </div>
    </div>

    <script src="https://cdn.socket.io/4.8.1/socket.io.min.js" crossorigin="anonymous"></script>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/chess.js/0.10.3/chess.min.js" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
    
    <script src="/js/chessgame.js"></script>
</body>
</html>